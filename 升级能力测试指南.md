# 升级能力测试指南

先做一轮 15 段的小测试，检查记录、真实 N-best、上下文重排和评测是否都能工作，再考虑扩大样本。这不是已经证明准确率提升的正式实验。

## 开始

在 Finder 打开本指南所在文件夹，双击 `开始升级能力测试.command`。它会启动本机 Ollama（若未运行），打开摄像头，并为每次测试新建独立的数据目录，避免覆盖或混入旧结果。

首次如遇权限提示，在系统设置的“隐私与安全”中允许实际运行终端的摄像头、辅助功能及需要的输入监控权限。允许后若未生效，重新打开终端并启动测试。

按终端的 `Next sample:` 提示录制。先是五条 normal，再是五条 low_voice，最后五条 silent。mode 是你遵照执行的采样标签；模型只读取图像，不读取声音，也不自动判断你用了哪种模式。

| 顺序 | 英文句子 |
|---|---|
| 1 | Please open the code editor |
| 2 | I want to save this file |
| 3 | Please go back to the previous page |
| 4 | Open the Chaplin project in Codex |
| 5 | Please write a message to my team |

- normal：正常出声说。
- low_voice：小声说。
- silent：完全不出声，只做自然口型。

每段按一下 Option 开始，说完再按一下 Option 停止；至少录制 2 秒，建议 3～6 秒。保持正面、嘴部清楚可见和相近的光线距离。每段后留约 15～20 秒给后台处理，可以减少排队，但这不是处理完成的保证。

本模式不会向其他应用键入预测结果。读错句子时不要把它当作正确参考；这一轮可用于熟悉流程，正式比较时另开一轮完整采集。过短或中断片段也会消耗该条提示并留存状态，评测会显示排除情况。

完成 15 条后，点击摄像头窗口按 q。等待终端出现“采集与处理已结束”，不要直接关掉终端，退出时会等待尚未完成的推理和重排。

## 评测

双击 `评测最近一次升级测试.command`。它会评测名称时间最新的一次 `upgrade_test_...` 会话，并打开结果文件夹；终端会明确显示本次评测的目录。它不会重跑模型，也不会重新录制。

用 Numbers 或文本查看器打开生成的文件：

- `metrics.csv`：`group=all` 是总体；normal、low_voice、silent 是分组。比较 `output=raw` 和 `output=llm` 的 wer、cer，越低越好。WER 0.2 表示 20% 词错误率，不能简单理解成整句准确率 80%。
- `samples.csv`：逐句对照 `ground_truth`、`raw_vsr`、`llm_output`，检查改对、改错、未改变的样本。
- `metrics.json`：查看排除样本原因与 `rerank_status_counts`。有 fallback 表示那条未成功重排，实际输出回退到 raw；不能算作成功重排证据。

每段只录一次，raw 与 llm 共用同一个真实视频。这比较的是“当前 Auto-AVSR top-1”与“升级后的 N-best 重排”；不是和原版 Chaplin 的单句自由改写 LLM 做完整 A/B。

## 检查真实候选与保存能力

回到该次会话目录，进入任意样本目录，应该看到：

- `video.mp4`：保留的录制片段。
- `sample.json`：参考句子、mode、speaker、camera、raw_vsr、llm_output、latency，以及解码/重排信息。
- `ctc_logits.npz`：可用时保存原始 CTC logits 和词表，普通使用不需要手动打开。

在 sample.json 中：

- `decode.hypotheses` 是 decoder 真实返回的候选；检查 `returned_nbest`，不保证所有视频一定有十条。
- 每条候选有 `candidate_id`、text、score、scores、token_ids；score 是原始路径分数。
- `rerank.rerank_scores` 是 Qwen 给每个 ID 的评分。
- `rerank.selected_id` 指向最终选中的真实候选。非 0 表示选择了原始 top-1 以外的候选；是否更好要看参考句子。
- 如果参考句子不在任何候选里，当前受约束的重排无法输出它。重复文本也可能来自不同 token 路径。

## 怎样判断有没有升级收益

1. 保存和可追溯能力：视频、标签、候选、原始分数、重排分数、选择和耗时是否齐全。
2. 重排收益：同一批有效样本中 llm WER/CER 是否低于 raw，逐句查看改善是否被其他句子的退步抵消。
3. 静默表现：单独看 silent 的 raw 与 llm，不能只看总分；normal/low_voice 是辅助对照。
4. 实用速度：查看 latency 的 vsr_ms、llm_ms、total_ms 和排队时间。各阶段为毫秒，除以 1000 得秒。

此次 context 固定为 Text editor、词汇 Chaplin/Codex，previous_text/history 随输出累积；参考句子没有传给 Qwen。程序中的 current_app 是用户提供的文字，并不会自动检测前台应用。

只做这一轮不能单独证明“自定义词汇有效”：那需要在完全相同的候选上分别使用空 context 与设置的 context 重排。重新录两次同时改 context 会混入录制差异。当前离线评测脚本只计算已保存输出，不会做这种重排对照。

每种模式只有 5 条，且句子重复、历史上下文也在累积；这适合初步检查，不足以证明稳定提升或严格比较三种模式。第一轮跑通后可扩大句子和重复次数，固定环境、模型与上下文规则再测。
