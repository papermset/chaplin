hydra-core >= 1.3.2
opencv-contrib-python >= 4.5.5.62
scipy >= 1.3.0
scikit-image >= 0.13.0
av >= 10.0.0
six >= 1.16.0 
mediapipe>=0.10.14,<0.10.21
torch
torchvision
torchaudio
ollama
pydantic
pynput
# Vendored ESPnet imports distutils; Python 3.12 needs its setuptools shim.
setuptools
